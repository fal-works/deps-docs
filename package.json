{
	"name": "@fal-works/deps-docs",
	"description": "A trivial tool that extracts README files from node_modules",
	"version": "0.1.0",
	"license": "Apache-2.0",
	"author": "FAL",
	"type": "module",
	"files": [
		"dist/"
	],
	"bin": {
		"deps-docs": "./dist/bin.js"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.1.3",
		"@types/node": "^24.1.0",
		"tsdown": "^0.13.0",
		"typescript": "^5.8.3"
	},
	"engines": {
		"node": ">=22.0.0"
	},
	"scripts": {
		"build": "tsdown",
		"lint": "biome check",
		"lint:fix": "biome check --fix",
		"check:types": "tsc --noEmit -p tsconfig.json",
		"check": "npm run check:types && npm run lint",
		"fix": "npm run check:types && npm run lint:fix"
	}
}
